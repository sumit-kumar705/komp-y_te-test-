[phases.setup]
nixPkgs = ['python39', 'postgresql']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build completed"']

[start]
cmd = 'gunicorn wsgi:app --workers 4 --bind 0.0.0.0:$PORT --timeout 120 --keep-alive 5 --log-level info --preload'
